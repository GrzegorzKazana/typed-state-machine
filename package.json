{
    "name": "typed-state-machine",
    "private": true,
    "scripts": {
        "init": "lerna bootstrap && lerna run build",
        "test": "lerna run --stream test"
    },
    "devDependencies": {
        "eslint": "^6.8.0",
        "lerna": "^3.20.2",
        "prettier": "^2.0.5"
    }
}
