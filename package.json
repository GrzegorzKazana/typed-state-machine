{
    "name": "typed-state-machine",
    "private": true,
    "scripts": {
        "init": "lerna bootstrap --hoist && lerna run build",
        "test": "lerna run --stream test",
        "publish": "lerna publish --message \"%v\""
    },
    "devDependencies": {
        "eslint": "^6.8.0",
        "lerna": "^3.20.2",
        "prettier": "^2.0.5"
    }
}
